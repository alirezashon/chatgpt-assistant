export const AI_PRIVACY_COPY = {
  consent:
    'External AI is opt-in. Conversation content is sent only after you choose a provider and accept processing for that provider.',
  localDefault:
    'Free Local works without external AI calls, accounts, or provider keys. Cached AI results stay in local extension storage until you clear them.',
  providerKeys:
    'User-owned provider keys are held only for the active extension session unless a secure backend vault is configured.',
} as const;
